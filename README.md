# lesbti-test

LesBTI 关系气场测试，一个基于 Vite、React 与 TypeScript 的本地题库驱动测试应用。

## 本地运行

```bash
pnpm install
pnpm dev
```

默认访问地址为 `http://localhost:5173/`。

## 构建与预览

```bash
pnpm build
pnpm preview
```

题库数据位于 `data/question-banks/lesbti-relationship-bank.v1.json`。
